@model IEnumerable<DonKawand.Models.ViewModels.Account.UserProfileVM>

    @{
        ViewBag.Title = "Dashboard";
    }

    <h2>Dashboard</h2>
    <h2>Subscribers</h2>



<table >
   
    <thead>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>EmailAddress</th>
            <th>UserName</th>
            <th>Password</th>
        </tr>
    </thead>
   
  
  @*@foreach (var item in Model)
    {
        <tr>
            
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
        </tr>
    }*@
</table>

@section scripts
{
    <script>

        $(document).ready(function () {

            $("#tblOrders").DataTable();

        });
    </script>
}